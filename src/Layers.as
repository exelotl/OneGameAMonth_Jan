package {
	public class Layers {
		
		private static var enum:int = 0;
		
		public static const
			GUI:int = enum++,
			FOREGROUND:int = enum++,
			PLAYER:int = enum++,
			LIVING_ENTITIES:int = enum++,
			SLOT:int = enum++,
			BACKGROUND:int = enum++;
			
		
	}
}